declare const supabase: any;

// This file provides a minimal ambient declaration so legacy files that
// reference `supabase` as a global variable will typecheck. The real
// instance is attached to window in `main.tsx`.
